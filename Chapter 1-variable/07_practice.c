// operation on 2 no.
#include <stdio.h>
int main()
{
    float a = 5.0;
    float b = 10.0;
    printf("addition is %.2f\n", a + b);
    printf("difference is %.2f\n", a - b);
    printf("multiplication is %.2f\n", a * b);
    printf("division is %.2f\n", a / b);
return 0;
}